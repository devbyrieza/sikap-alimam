name: Auto-Deploy Trigger (Unlimited Free)

on:
  push:
    branches:
      - main
      - master

jobs:
  notify-deploy:
    name: Auto Deploy sikap-alimam
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Trigger Deployment Event
        run: |
          echo "Push detected on branch ${{ github.ref_name }} for sikap-alimam."
          echo "Coolify server deployment sequence initiated successfully."
